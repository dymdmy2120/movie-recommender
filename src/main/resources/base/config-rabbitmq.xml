<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/task 
		http://www.springframework.org/schema/task/spring-task.xsd
		http://www.springframework.org/schema/tx 
		http://www.springframework.org/schema/tx/spring-tx.xsd
		http://www.springframework.org/schema/rabbit
		http://www.springframework.org/schema/rabbit/spring-rabbit-1.4.xsd">
    	
    <task:executor id="mqConnExecutor" pool-size="5-10" queue-capacity="500" />
		
    <bean id="simpleMessageConverter" class="org.springframework.amqp.support.converter.SimpleMessageConverter"/>
	<bean id="jsonMessageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter"/>
	
	<bean id="rcf" class="com.rabbitmq.client.ConnectionFactory">
        <property name="automaticRecoveryEnabled" value="true" />
        <property name="topologyRecoveryEnabled" value="true" />
        <property name="networkRecoveryInterval" value="10000" />
        <property name="connectionTimeout" value="10000" />
        <property name="requestedHeartbeat" value="10" />
	</bean>
		
	<rabbit:connection-factory id="connectionFactory" host="${rabbitmq.host}" port="${rabbitmq.port}"
		username="${rabbitmq.username}" password="${rabbitmq.password}" virtual-host="${rabbitmq.virtualHost}"
		channel-cache-size="50" executor="mqConnExecutor" connection-factory="rcf" />
		
	<rabbit:admin id="rabbitAdmin" connection-factory="connectionFactory" />

	<rabbit:template id="jsonRabbitTemplate" connection-factory="connectionFactory" message-converter="jsonMessageConverter"/>
	<rabbit:template id="simpleRabbitTemplate" connection-factory="connectionFactory" message-converter="simpleMessageConverter"/>

	<rabbit:queue id="Goods_CronJob_SeatLockAndDraw_Queue" name="Goods_CronJob_SeatLockAndDraw_Queue" durable="true" auto-delete="false" />
	
	<rabbit:fanout-exchange id="GoodsCenterPostProcessEx" name="GoodsCenterPostProcessEx" durable="true" auto-delete="false">  
		<rabbit:bindings>  
			<rabbit:binding queue="Goods_CronJob_SeatLockAndDraw_Queue" />
		</rabbit:bindings>  
    </rabbit:fanout-exchange>

	<bean id="SeatLockAndDrawQListener" class="com.wepiao.goods.mq.SeatLockAndDrawQListener" />

	<rabbit:listener-container connection-factory="connectionFactory" message-converter="jsonMessageConverter" 
		concurrency="5" max-concurrency="10" acknowledge="manual">
	  <rabbit:listener ref="SeatLockAndDrawQListener" method="onMessage" queue-names="Goods_CronJob_SeatLockAndDraw_Queue" />
	</rabbit:listener-container>

</beans>
